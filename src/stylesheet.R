# stylesheet
heading <- list(textAlign = 'center',
                color = 'white',
                backgroundColor = "black")
panel_title <- list(
  textAlign='left'
)

card_title <- list(
  textAlign='center'
)

card_text <- list(
  textAlign='center'
)

lp_chart_style <- list(
  borderWidth="0",
  width="100%",
  height="300px"
)

radio_buttons_style <- list(
  paddingTop="10px",
  paddingBottom="10px",
  paddingLeft="10px",
  backgroundColor="gray",
  color="white"
)